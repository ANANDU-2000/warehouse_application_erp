# new-app (target)

Target stack: **React + TypeScript** frontend, **Node.js + Express + TypeScript** backend, **Microsoft SQL Server**.

## Status

**Phase 1 analysis is in progress.** Do not implement feature code until the module's `docs/modules/<name>.md` has a Review PASS.

- Login analysis: `docs/modules/login.md`
- Placeholder folders under `frontend/` and `backend/` are structural only (`.gitkeep`). No `.tsx` / Express routes yet.

## Layout (planned)

See [docs/TARGET_STRUCTURE.md](docs/TARGET_STRUCTURE.md).

## Rules

- Source of truth remains `../../source-app/` until a module is migrated.
- Never edit `source-app/`.
- One module at a time after Analyze → Review → Implement → Test → Compare → Stop.
