# Repositories (Phase 3.2)

Table/aggregate repositories that talk to SQL Server via `mssql` will live here.

Do **not** invent repositories ahead of Phase 3.2. Map to ORM tables from `docs/20_Database_Analysis.md` and DDL under `../database/ddl/`.
