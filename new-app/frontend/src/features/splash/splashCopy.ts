/**
 * Exact Splash WIRE copy — splash_page.dart
 */

export const SPLASH_SESSION_REFRESH_ERROR =
  "We couldn't refresh your session. Check your connection and tap Retry.";

export const SPLASH_WARMUP_RETRY =
  "Server is warming up. Retrying in 10 seconds…";

export const SPLASH_RETRY_LABEL = "Retry";

export const SPLASH_USE_ANOTHER_ACCOUNT = "Use another account";

/** Web restore timeout (kIsWeb). */
export const SPLASH_RESTORE_TIMEOUT_MS = 8000;

/** Delay before one warmup retry. */
export const SPLASH_WARMUP_DELAY_MS = 10000;

/** Flutter jwt access skew before refresh (seconds). */
export const SPLASH_ACCESS_SKEW_SEC = 90;
