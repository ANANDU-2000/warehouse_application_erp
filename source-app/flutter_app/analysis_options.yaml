include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Rebuild v3 prioritizes UX correctness; keep CI signal on real issues.
    # "const everywhere" and a few legacy deprecations are intentionally non-fatal.
    prefer_const_constructors: false
    use_build_context_synchronously: false
    avoid_print: false
